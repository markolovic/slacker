<h2>My Classes</h2>

<ul id="classes-list">
  <% @classes.each do |c| %>
      <li>
        <h4><%= link_to c.name, "/courses/#{c.id}" %></h4>  
      </li>
    <% end %>
</ul>
<br>
<p><%= link_to "Add new class", new_course_path %></p>
